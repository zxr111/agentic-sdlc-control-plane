apiVersion: v1
kind: Service
metadata:
  name: ai-sdlc-factory-api
spec:
  selector:
    app.kubernetes.io/name: ai-sdlc-factory
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
